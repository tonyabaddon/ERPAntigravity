displayName: "Backend /api/v1/live"
monitoredResource:
  type: uptime_url
  labels:
    host: garindo-jaya-panel-msme-erp-xnrhcw7onq-as.a.run.app
    project_id: gen-lang-client-0410251117
httpCheck:
  path: /api/v1/live
  port: 443
  useSsl: true
  validateSsl: true
  requestMethod: GET
period: 60s
timeout: 10s
checkerType: STATIC_IP_CHECKERS

# Resource name (created 2026-07-17):
# projects/gen-lang-client-0410251117/uptimeCheckConfigs/backend-api-v1-live-406wNLiEo3Y
#
# Used by: alert-policies/alert-4-backend-uptime.yaml
#
# Why /api/v1/live (not /api/v1/ready):
# /api/v1/ready checks DB connectivity and returns 503 if DB is unreachable.
# /api/v1/live is the correct liveness indicator — DB issues are covered by Alert 3.
#
# To re-create:
#   gcloud monitoring uptime create "Backend /api/v1/live" \
#     --resource-type=uptime-url \
#     --resource-labels="host=garindo-jaya-panel-msme-erp-xnrhcw7onq-as.a.run.app,project_id=gen-lang-client-0410251117" \
#     --path=/api/v1/live \
#     --port=443 \
#     --protocol=https \
#     --validate-ssl=true \
#     --period=1 \
#     --timeout=10 \
#     --project=gen-lang-client-0410251117
